import { FastifyInstance } from "fastify";

export async function paymentsRoutes(fastifyApp: FastifyInstance){
    // receive events payments
    // fastifyApp.post('/webhook', ReceiveEventsPayments)   
}